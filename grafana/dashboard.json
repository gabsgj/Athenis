{
  "title": "Legal Simplifier Dashboard",
  "panels": [
    {
      "type": "graph",
      "title": "Request Latency",
      "targets": [{"expr": "histogram_quantile(0.9, sum(rate(request_latency_seconds_bucket[5m])) by (le))"}]
    },
    {
      "type": "singlestat",
      "title": "GPU Memory Used",
      "targets": [{"expr": "gpu_memory_used_bytes"}]
    }
  ]
}
